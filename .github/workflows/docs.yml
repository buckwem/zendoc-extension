# Copyright (c) 2026 Mark Buckwell and contributors
# SPDX-License-Identifier: MIT

name: Documentation
on:
  push:
    branches:
      - main
  # Deliberately *no* `release: [published]` trigger, though the cover
  # page's "Release: <tag>" line does need a rebuild once the tag exists.
  # That rebuild now comes from release-redeploy.yml, which re-triggers
  # this workflow against `main`.
  #
  # Deploying straight from the release event is what the old trigger did,
  # and it never worked: a release event runs against `refs/tags/<tag>`, so
  # the Pages deployment carried a tag ref, and Pages - configured with
  # `source: {branch: main}` - accepted it, reported `success`, and carried
  # on serving the previous build. Silently, on every release from 0.17.0
  # to 0.17.2 (prodockit-extensions#147).
  workflow_dispatch:

# Publishing a release right after merging its version bump starts two
# deploys at once - one for the push, one for the release - and without a
# concurrency group they race. That happened on 0.15.1: the push run had
# checked out `main` before the tag existed, so its build saw the *previous*
# tag, and its deployment is the one that ended up live, even though the
# release run started later, finished later, and reported success. The site
# sat on the old release number until a manual redeploy.
#
# `cancel-in-progress: false` rather than true: a queued deploy should wait
# and then supersede, not be thrown away. GitHub's own Pages starter
# workflow uses this same pairing.
concurrency:
  group: pages
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write
# The runner image's own `pandoc` package lags well behind upstream -
# `ubuntu-24.04` currently ships 3.1.3, while a contributor on macOS or
# Windows has whatever Homebrew or winget last gave them. That gap is not
# cosmetic: pandoc 3.1.3 accepted `<pre><code>` containing element markup
# as a code block and 3.10 does not, so every fenced code block in the PDF
# reflowed as justified prose on a current pandoc while CI kept publishing
# perfect output (prodockit-extensions#207). CI could not see the bug, and
# would have started producing it unannounced at the next image bump.
#
# Pinning an upstream release keeps CI and local development on one
# version, and makes a pandoc change an explicit commit rather than a
# silent inheritance.
#
# prodockit-template pins the same way and currently says 3.10; it and
# prodockit-userguide's .gitlab-ci.yml both need bringing to this number,
# so the family builds its PDFs with one pandoc rather than three
# (prodockit-extensions#209).
env:
  PANDOC_VERSION: "3.10.1"

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    # Pinned rather than ubuntu-latest, for the same reason zensical and
    # weasyprint are pinned below: this job publishes an artifact, and its
    # inputs should move when someone decides they should.
    #
    # The image is the input pip cannot reach. `pandoc` comes from it (and
    # distribution packages lag upstream far enough that some markdown edge
    # cases parse differently), as do the Inter/JetBrains Mono fonts the
    # PDF embeds and the base Chrome renders Mermaid against. On
    # ubuntu-latest all three move the day GitHub migrates the label, with
    # nothing committed - the same silent-change failure the package pins
    # exist to prevent, one layer down.
    #
    # ubuntu-latest already *is* 24.04 today, so this changes nothing now.
    # It only takes effect at the migration, which is exactly when a
    # documentation build wants to be told rather than surprised.
    #
    # The cost: pinned images are retired roughly a year after the next
    # LTS, and the job then fails outright instead of drifting. That is the
    # right failure - loud, and at a time of your choosing.
    runs-on: ubuntu-24.04
    # Builds the site and both PDFs: about four minutes, twelve at
    # its slowest. The margin is for a slow build, not for a stall -
    # this job sat 35 minutes on `apt-get` once, and would have held
    # the runner for six hours (#478).
    timeout-minutes: 30
    # Consumed by the verify job below.
    outputs:
      page_url: ${{ steps.deployment.outputs.page_url }}
      index_sha: ${{ steps.fingerprint.outputs.index_sha }}
    env:
      # mermaid-cli drives Chrome through Puppeteer to rasterise diagrams.
      # Point it at the apt-installed Chrome below rather than letting
      # Puppeteer download its own, and skip that download entirely.
      #
      # PUPPETEER_SKIP_DOWNLOAD, not the older
      # PUPPETEER_SKIP_CHROMIUM_DOWNLOAD - Puppeteer renamed it, and
      # mermaid-cli ^11.4.2 resolves to puppeteer 25.x, which only honours
      # the new name. Under the old name the skip silently does nothing and
      # a full Chrome build is downloaded on every run.
      PUPPETEER_SKIP_DOWNLOAD: "true"
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/google-chrome-stable
    steps:
      - uses: actions/configure-pages@v5
      - uses: actions/checkout@v5
        with:
          # actions/checkout's default shallow clone (fetch-depth: 1) fetches
          # no tags at all, so prodockit.zensical_macros' {{ release }}
          # variable (git describe --tags --abbrev=0) always returned ""
          # here - the cover page's "Release: ..." line silently never
          # appeared on the deployed site/PDF, even though it worked in any
          # full local checkout. Full history needed so the nearest tag
          # reachable from HEAD is actually present to describe.
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version-file: .python-version
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      # The image's mirrorlist puts `azure.archive.ubuntu.com` first. When
      # that host is down - it was, repeatedly, on 2026-08-18 - apt spends
      # minutes on it before reaching the `archive.ubuntu.com` fallback,
      # which answers in milliseconds throughout. Neither a shorter
      # connect timeout nor fewer retries reached it in reasonable time,
      # so the dead host is taken out of the list instead (#478).
      #
      # Reported rather than assumed: if the image stops shipping a
      # mirrorlist, or stops using Azure, the run says so and carries on
      # unchanged - a substitution that quietly matched nothing would be
      # worse than either.
      - name: Point apt at a mirror that answers
        timeout-minutes: 2
        run: |
          list=/etc/apt/apt-mirrors.txt
          if [ ! -f "$list" ]; then
            echo "::notice::no $list in this image - apt left as it is"
          elif ! grep -q 'azure.archive.ubuntu.com' "$list"; then
            echo "::notice::$list no longer names Azure - apt left as it is"
          else
            sudo sed -i 's|http://azure.archive.ubuntu.com/ubuntu|https://archive.ubuntu.com/ubuntu|g' "$list"
            echo "mirrorlist now:"; cat "$list"
          fi
      # A mirror that stalls must fail fast, not hold the runner: this
      # step takes seconds, and once sat 35 minutes (#478).
      #
      # The image lists `azure.archive.ubuntu.com` first and
      # `https://archive.ubuntu.com` as a fallback. When the first is
      # dead the fallback works - but apt waits out a 120-second connect
      # timeout per source before trying it, four sources deep. Ten
      # seconds is far above a healthy mirror's response and reaches the
      # fallback in a fraction of the time.
      #
      # Retries are deliberately not raised: they re-ask the dead mirror
      # rather than moving on, which is what exhausted the budget when
      # this was first tried.
      - run: sudo apt-get -o Acquire::http::Timeout=10 update && sudo apt-get -o Acquire::http::Timeout=10 install -y curl fonts-inter fonts-jetbrains-mono
        timeout-minutes: 8
      # Upstream release rather than the image's package - see PANDOC_VERSION.
      #
      # Cached because the release CDN is the single most fragile thing in
      # this job: it returned 503 for hours one evening and failed every
      # run in the matrix before a single test executed. The key carries
      # the version, so a bump fetches afresh and an unchanged pin never
      # touches the network again.
      - id: pandoc-cache
        uses: actions/cache@v4
        with:
          path: /tmp/pandoc.deb
          key: pandoc-${{ env.PANDOC_VERSION }}-amd64-deb
      # The retry stays for the first run on a new key, and for the day
      # the cache is evicted - a cache is a saving, not a guarantee.
      - if: steps.pandoc-cache.outputs.cache-hit != 'true'
        run: |
          curl -fsSL --retry 5 --retry-all-errors --retry-delay 5 --connect-timeout 20 -o /tmp/pandoc.deb "https://github.com/jgm/pandoc/releases/download/${PANDOC_VERSION}/pandoc-${PANDOC_VERSION}-1-amd64.deb"
      - run: sudo apt install -y /tmp/pandoc.deb
      # mermaid-cli, so the ```mermaid diagram in extensions/bibliography.md
      # is pre-rendered to a static SVG for the PDF - WeasyPrint has no JS
      # engine to run Mermaid.js the way the live site does, and without
      # this the PDF shows raw `flowchart LR ...` source instead.
      - run: curl -fsSL https://dl.google.com/linux/linux_signing_key.pub | gpg --dearmor | sudo tee /usr/share/keyrings/google-chrome.gpg > /dev/null
      - run: echo "deb [arch=amd64 signed-by=/usr/share/keyrings/google-chrome.gpg] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
      # A mirror that stalls must fail fast, not hold the runner: this
      # step takes seconds, and once sat 35 minutes (#478).
      #
      # The image lists `azure.archive.ubuntu.com` first and
      # `https://archive.ubuntu.com` as a fallback. When the first is
      # dead the fallback works - but apt waits out a 120-second connect
      # timeout per source before trying it, four sources deep. Ten
      # seconds is far above a healthy mirror's response and reaches the
      # fallback in a fraction of the time.
      #
      # Retries are deliberately not raised: they re-ask the dead mirror
      # rather than moving on, which is what exhausted the budget when
      # this was first tried.
      - run: sudo apt-get -o Acquire::http::Timeout=10 update && sudo apt-get -o Acquire::http::Timeout=10 install -y google-chrome-stable
        timeout-minutes: 8
      - run: npm ci --prefix tools/mermaid
      # MathJax, the same arrangement for TeX maths. These docs carry no
      # formulas today, so this renders nothing yet - it is here so that the
      # first page to add one is rendered rather than silently published as
      # raw LaTeX, the way the Mermaid diagram above once was. Needs no
      # browser: tex2svg.js is plain Node.
      - run: npm ci --prefix tools/mathjax
      # The [testing] extra brings pytest/pymupdf for the built-output
      # checks below. pymupdf is also what extra.pdf_include_index needs to
      # read page numbers back out of the first pass, so this one install
      # covers both - no separate [index] extra needed here.
      #
      # zensical is pinned exactly *here* rather than in pyproject.toml,
      # which deliberately declares only a floor (>=0.0.53): prodockit is a
      # library, so an exact pin in its metadata would propagate to every
      # consumer and conflict with any project needing a different Zensical.
      # This is the build side, where the opposite applies - the site and
      # PDF published from this job are artifacts, and they should change
      # because someone decided to change them, not because a transitive
      # resolution moved overnight. 0.0.52 silently redrew the GitHub brand
      # icon (Font Awesome 7.2.0 -> 7.3.1) on every page; the PDF was
      # byte-identical.
      #
      # weasyprint is pinned for a sharper reason than zensical: it decides
      # pagination. A release that lays out one paragraph differently shifts
      # every page number after it - and those page numbers are content
      # here, resolved into the back-of-book index and the Table of
      # Contents. That is a silently wrong document, not a failed build.
      #
      # Raising any of them is deliberate work: bump here (and zensical and
      # markdown also in pyproject.toml's floors), rebuild, and diff the
      # built site and PDF against the previous version before committing.
      # drift.yml watches for newer releases weekly and opens an issue when
      # one would change the output.
      #
      # Markdown and pymdown-extensions are pinned because a floor is not
      # a pin. prodockit declares both as dependencies - markdown always,
      # pymdown-extensions since `prodockit.steps` was built on its Blocks
      # API (#378) - and zensical declares floors for them too. A floor
      # resolves to whatever is newest on the day the job runs, so a
      # release of either could change the rendered HTML on every page and
      # this workflow would publish it, green, with nothing committed
      # (prodockit-extensions#178).
      #
      # The floors zensical declares for them are `markdown>=3.7` and
      # `pymdown-extensions>=11.0`. Naming them here is safe: `prodockit
      # pins` skips comments when it scans, so a specifier written in prose
      # is not mistaken for a declaration and `--set` will not rewrite this
      # sentence (prodockit-extensions#184). It was not safe before that
      # fix, which is why this comment previously named the versions
      # without their operators.
      #
      # That is not theoretical for this project in particular.
      # prodockit.pdf.css and prodockit.pdf.lua both match on the specific
      # class shapes pymdownx emits - `pymdownx.blocks.tab`'s tabbed sets,
      # arithmatex's `<div|span class="arithmatex">` - so the renderer is an
      # input to the PDF's own correctness, not just to the website's
      # appearance.
      - run: pip install -e ".[testing]" "zensical==0.0.61" "weasyprint==69.0" "Markdown==3.10.3" "pymdown-extensions==11.0.2"
      - run: prodockit init-mathjax --no-gitignore
      # The PDF renderer consumes this completed site; it never invokes
      # Zensical itself or cleans the generated output.
      - run: python tools/docs_page_pdfs.py check
      - run: zensical build --clean --strict
      # Two pandoc+WeasyPrint passes when a selected document contains index
      # terms: the back-of-book index can only be built once WeasyPrint has
      # laid the document out (see prodockit.pdf.index). The reviewed matrix
      # includes index-terms.md deliberately, so that behavior is exercised
      # through the distinct single-page path as well as the complete PDF.
      - run: prodockit pdf
      - name: Build reviewed single-page PDF matrix
        run: |
          python - <<'PY'
          import subprocess
          import tomllib

          from tools.docs_page_pdfs import publish

          with open(".github/docs-single-page-pdfs.toml", "rb") as source:
              matrix = tomllib.load(source)
          for group, paths in matrix["build"].items():
              for path in paths:
                  print(f"::group::{group}: {path}", flush=True)
                  subprocess.run(["prodockit", "pdf", "-m", path], check=True)
                  publish(path)
                  print("::endgroup::", flush=True)
          PY
      - run: prodockit update-dates
      # Checks what the builds above actually produced, using this project's
      # own pytest plugin - deselected from the normal unit run, which needs
      # no build. Catches a Mermaid diagram or formula reaching the PDF as
      # raw source, and a nav page missing from the site, before either is
      # published (see tests/test_built_docs.py).
      - run: python -m pytest tests/test_built_docs.py -m built -v
      # Everything above validates the reusable, analytics-free document.
      # Only the canonical prodockit.org deployment gets the measurement ID:
      # it arrives through Actions' secret store and is written into an
      # ignored temporary config, never into the repository or a build run
      # from a fork.
      - name: Prepare canonical website analytics
        if: github.repository == 'buckwem/prodockit-extensions'
        env:
          GOOGLE_ANALYTICS_ID: ${{ secrets.GOOGLE_ANALYTICS_ID }}
        run: |
          if [ -z "$GOOGLE_ANALYTICS_ID" ]; then
            echo "::error::The prodockit.org deployment needs the GOOGLE_ANALYTICS_ID repository secret."
            exit 1
          fi
          python tools/canonical_site_config.py zensical.toml .zensical-canonical.toml "$GOOGLE_ANALYTICS_ID"
      - name: Build the canonical prodockit.org website
        if: github.repository == 'buckwem/prodockit-extensions'
        run: |
          zensical build --config-file .zensical-canonical.toml --clean --strict
          prodockit update-dates --config-file .zensical-canonical.toml
      - name: Check the canonical website has consent-gated analytics
        if: github.repository == 'buckwem/prodockit-extensions'
        env:
          GOOGLE_ANALYTICS_ID: ${{ secrets.GOOGLE_ANALYTICS_ID }}
        run: |
          grep -RFlq --include='*.html' "$GOOGLE_ANALYTICS_ID" site
          grep -RFlq --include='*.html' 'href="#__consent"' site
      - name: Check every PDF action before publication
        run: python tools/docs_page_pdfs.py verify
      # Fingerprint what we are about to publish, so the verify job below can
      # tell whether the live site is actually serving *this* build. Taken
      # from the same directory upload-pages-artifact packs, immediately
      # before it packs it.
      - id: fingerprint
        run: echo "index_sha=$(sha256sum site/index.html | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-pages-artifact@v4
        with:
          path: site
      - uses: actions/deploy-pages@v4
        id: deployment

  # A successful deploy does not mean the site serves it. Three of the four
  # releases up to 0.17.1 published fine and then kept serving the previous
  # build - deployment reported `success`, the superseded one reported
  # `inactive`, and the origin carried on returning the old bytes until
  # someone noticed and re-ran this workflow by hand. Nothing in the run
  # itself was wrong, so nothing went red.
  #
  # This closes that gap: compare the live page against the fingerprint of
  # the build we just uploaded, and fail the run if they never converge.
  # Separate job rather than another step, so the deploy itself is never
  # gated on delivery - the artifact is published either way, and this only
  # decides whether the run reports success.
  verify:
    needs: deploy
    runs-on: ubuntu-24.04
    # Deliberately above the polling window below: this job waits
    # out Pages' 10-minute cache for up to 15 minutes, so a timeout
    # under that would cut short a wait that is working (#478).
    timeout-minutes: 25
    steps:
      # Pages serves `cache-control: max-age=600`, so up to ~10 minutes of
      # staleness is normal and expected rather than a fault. 15 minutes of
      # polling clears that window with room to spare; past it, every
      # occurrence so far has needed a manual redeploy. A `?cb=` query buster
      # would not help - the CDN normalises query strings - so this waits the
      # TTL out rather than trying to defeat it.
      - name: Verify the live site serves this build
        env:
          PAGE_URL: ${{ needs.deploy.outputs.page_url }}
          WANT: ${{ needs.deploy.outputs.index_sha }}
        run: |
          set -u
          for attempt in $(seq 1 30); do
            # Download to a file rather than piping or capturing. Piping into
            # sha256sum reports the *pipe's* exit status, so a failed fetch
            # silently hashes empty input instead of being noticed; capturing
            # the body in $(...) strips its trailing newline, so the hash
            # would never match the file on disk.
            if curl -fsSL --max-time 30 -o live-index.html "$PAGE_URL"; then
              got=$(sha256sum live-index.html | cut -d' ' -f1)
            else
              got="fetch-failed"
            fi
            if [ "$got" = "$WANT" ]; then
              echo "Live after ~$(( (attempt - 1) * 30 ))s: $PAGE_URL is serving this build."
              exit 0
            fi
            echo "attempt $attempt/30: live index.html is $got, want $WANT"
            sleep 30
          done
          # One line: a ::error:: annotation is terminated by the newline, and
          # a backslash continuation would fold this block's own indentation
          # into the message as runs of spaces.
          echo "::error::$PAGE_URL is not serving this build after 15 minutes. The deploy succeeded but delivery did not - this has happened on most releases so far. Re-run with: gh workflow run docs.yml --ref main"
          echo "Live page headers for diagnosis (last-modified predating this run means the old build is still being served):"
          curl -sI "$PAGE_URL" | grep -iE 'last-modified|etag|age' || true
          exit 1
